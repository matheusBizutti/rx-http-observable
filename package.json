{
  "name": "rx-http-observable",
  "version": "0.0.8",
  "description": "Http client for Javascript Projects",
  "author": "matheus.mbizutti@gmail.com",
  "scripts": {
    "prebuild": "npm i",
    "build": "tsc -p ./tsconfig.json && cp package.json dist && cp package-lock.json dist && cp README.md dist && cd dist && npm ci && npm pack"
  },
  "typings": "./dist/index.d.ts",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^3.3.1"
  },
  "dependencies": {
    "axios": "0.18.0",
    "rxjs": "6.4.0",
    "rxjs-compat": "6.4.0"
  },
  "repository": "https://github.com/matheusBizutti/rx-http"
}
