{
  "name": "rx-http",
  "version": "0.0.1",
  "description": "Http client for Javascript Projects",
  "author": "matheus.mbizutti@gmail.com",
  "scripts": {
    "prebuild": "npm i",
    "build": "tsc -p ./tsconfig.json && cp package.json dist/ && cp README.md dist/ && cd dist && npm pack"
  },
  "files": [
    "package.json",
    "README.md"
  ],
  "license": "MIT",
  "devDependencies": {
    "typescript": "^3.3.1"
  },
  "dependencies": {
    "axios": "0.18.0",
    "rxjs": "6.4.0",
    "rxjs-compat": "6.4.0"
  }
}
